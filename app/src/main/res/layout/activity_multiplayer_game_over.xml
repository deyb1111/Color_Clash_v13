<?xml version="1.0" encoding="utf-8"?>
<!--
    Multiplayer Game Over — mirrors activity_game_over.xml so the
    "post-match" experience is identical between offline and online
    play. Same dark background, GAME OVER caption, big result, score
    + gold summary, and an indigo "Main Menu" CTA at the bottom.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0D0D1A"
    android:fitsSystemWindows="true"
    android:paddingStart="28dp"
    android:paddingEnd="28dp"
    android:paddingTop="12dp"
    android:paddingBottom="14dp">

    <LinearLayout
        android:id="@+id/mp_game_over_content"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/mp_btn_menu"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/mp_game_over_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="GAME OVER"
            android:textSize="16sp"
            android:textColor="#9E9EBD"
            android:letterSpacing="0.18" />

        <TextView
            android:id="@+id/mp_result_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Result"
            android:textSize="36sp"
            android:textStyle="bold"
            android:gravity="center"
            android:singleLine="true"
            android:ellipsize="end"
            android:maxWidth="560dp"
            android:shadowColor="#AAFFAA00"
            android:shadowDx="0"
            android:shadowDy="0"
            android:shadowRadius="10"
            android:layout_marginTop="8dp" />

        <TextView
            android:id="@+id/mp_score_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Scores"
            android:textSize="18sp"
            android:textColor="#CCCCCC"
            android:gravity="center"
            android:maxLines="3"
            android:ellipsize="end"
            android:layout_marginTop="16dp" />

        <TextView
            android:id="@+id/mp_gold_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Gold earned"
            android:textSize="15sp"
            android:textColor="#FFDD22"
            android:gravity="center"
            android:maxLines="2"
            android:ellipsize="end"
            android:shadowColor="#AAFFAA00"
            android:shadowDx="0"
            android:shadowDy="0"
            android:shadowRadius="6"
            android:layout_marginTop="10dp" />

    </LinearLayout>

    <!-- Main Menu CTA: width scales with screen via constraintWidth_percent
         instead of hard 70dp side margins. -->
    <Button
        android:id="@+id/mp_btn_menu"
        android:layout_width="0dp"
        android:layout_height="50dp"
        android:text="🏠  Main Menu"
        android:textSize="15sp"
        android:textStyle="bold"
        android:textAllCaps="false"
        android:textColor="#FFFFFF"
        android:backgroundTint="#5C6BC0"
        app:cornerRadius="12dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.55"
        app:layout_constraintWidth_max="520dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
